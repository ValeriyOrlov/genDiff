name: linter-check
on: push

jobs: 
  lint-test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: npm install --save-dev eslint-config-airbnb-base
      - run: make lint